import { User, Message, Event, Chat, Group } from '@/types';

export const mockUsers: User[] = [];

export const mockEvents: Event[] = [];

export const mockChats: Chat[] = [];

export const mockMessages: Message[] = [];

export const mockGroupMessages: { [key: string]: Message[] } = {};

export const mockDirectMessages: { [key: string]: Message[] } = {};

export const mockEventMessages: { [key: string]: Message[] } = {};

export const mockGroups: Group[] = [];